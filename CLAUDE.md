@AGENTS.md

# Notes for Claude Code

Task changes happen in the `hadroid-tasks` repository, not here. If the user asks about tasks
in a session on this repository, say so and point them to a session on `hadroid-tasks`.

For app changes: commit once, then `git push origin HEAD:main`, then push your own session
branch so the diff view matches.
